{{- range $appset := .Values.appsets }}
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: {{ $appset.name }}-appset
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: "{{ $appset.url }}"
        revision: "{{ $appset.revision | default "HEAD" }}"
        files:
          - path: "{{ $appset.name }}/**/app-config.yaml"
  template:
    metadata:
      name: "{{`{{ .name }}`}}"
      labels:
        app.kubernetes.io/name: "{{`{{ .name }}`}}"
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{`{{ .namespace }}`}}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          selfHeal: true
  templatePatch: |
    spec:
      sources:
        - repoURL: {{`{{ .source.repoURL }}` | default $appset.url }}
          targetRevision: {{`{{ .source.targetRevision }}` | default $appset.revision }}
          {{`{{- if hasKey .source "chart" }}`}}
          chart: {{`{{ .source.chart }}`}}
          {{`{{- else if hasKey .source "path" }}`}}
          path: {{`{{ .source.path }}`}}
          {{`{{- end }}`}}
{{- end }}
